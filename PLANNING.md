## Plans

We probably need to be able to fetch the API of a repo.

Need to vibe code out a [redis rs] compatible in-memory store.

Could we do the same thing with SQLite?

The goal is a single executable that utilizes every CPU core to its fullest
